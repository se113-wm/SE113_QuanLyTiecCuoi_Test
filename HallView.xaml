<UserControl
    x:Class="QuanLyTiecCuoi.HallView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:local="clr-namespace:QuanLyTiecCuoi"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Tên trang  -->
        <TextBlock
            Grid.Row="0"
            Margin="10"
            HorizontalAlignment="Center"
            FontSize="20"
            FontWeight="Bold"
            Text="Quản lý danh sách sảnh" />

        <!--  Thông tin chi tiết  -->
        <materialDesign:Card
            Grid.Row="1"
            Margin="10"
            Padding="10"
            BorderThickness="0.5">
            <StackPanel>
                <TextBlock
                    Margin="0,0,0,10"
                    FontSize="14"
                    FontWeight="Bold"
                    Text="Thông tin chi tiết" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>

                    <!--  Tên sảnh  -->
                    <TextBox
                        Grid.Column="0"
                        Margin="5"
                        materialDesign:HintAssist.Hint="Tên sảnh"
                        Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                        Text="{Binding TenSanh, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <!--  Loại sảnh (ComboBox)  -->
                    <ComboBox
                        Grid.Column="1"
                        Margin="5"
                        materialDesign:HintAssist.Hint="Loại sảnh"
                        DisplayMemberPath="TenLoaiSanh"
                        ItemsSource="{Binding HallTypes}"
                        SelectedItem="{Binding SelectedHallType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource MaterialDesignFloatingHintComboBox}" />

                    <!--  Đơn giá bàn tối thiểu (readonly)  -->
                    <TextBox
                        Grid.Column="2"
                        Margin="5"
                        materialDesign:HintAssist.Hint="Đơn giá bàn tối thiểu"
                        IsReadOnly="True"
                        Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                        Text="{Binding SelectedHallType.DonGiaBanToiThieu, StringFormat=N0}" />

                    <!--  Số lượng bàn tối đa  -->
                    <TextBox
                        Grid.Column="3"
                        Margin="5"
                        materialDesign:HintAssist.Hint="Số lượng bàn tối đa"
                        Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                        Text="{Binding SoLuongBanToiDa, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>
                <!--  Ghi chú  -->
                <TextBox
                    Margin="5"
                    materialDesign:HintAssist.Hint="Ghi chú"
                    Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                    Text="{Binding GhiChu, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </materialDesign:Card>

        <!--  Nút & tìm kiếm  -->
        <materialDesign:Card
            Grid.Row="2"
            Margin="10"
            Padding="10"
            BorderThickness="0.5">
            <StackPanel>
                <TextBlock
                    Margin="0,0,10,0"
                    FontSize="14"
                    FontWeight="Bold"
                    Text="Thao tác" />
                <DockPanel VerticalAlignment="Top" LastChildFill="False">
                    <!--  Tìm kiếm  -->
                    <StackPanel
                        VerticalAlignment="Top"
                        DockPanel.Dock="Left"
                        Orientation="Horizontal">
                        <ComboBox
                            Width="200"
                            Margin="5"
                            materialDesign:HintAssist.Hint="Tìm theo"
                            ItemsSource="{Binding SearchProperties}"
                            SelectedItem="{Binding SelectedSearchProperty, Mode=TwoWay}"
                            Style="{StaticResource MaterialDesignOutlinedComboBox}" />
                        <TextBox
                            Width="300"
                            Margin="5"
                            materialDesign:HintAssist.Hint="Tìm kiếm..."
                            Style="{StaticResource MaterialDesignOutlinedTextBox}"
                            Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>

                    <StackPanel
                        Margin="0,0,0,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        DockPanel.Dock="Right"
                        Orientation="Horizontal">
                        <StackPanel Width="110" Margin="0,0,20,0">
                            <Button
                                Width="100"
                                Margin="5"
                                Command="{Binding AddCommand}"
                                Content="Thêm"
                                Style="{StaticResource MaterialDesignRaisedButton}" />
                            <TextBlock
                                Height="32"
                                FontSize="12"
                                Foreground="Gray"
                                Text="{Binding AddMessage}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                        <StackPanel Width="110" Margin="0,0,20,0">
                            <Button
                                Width="100"
                                Margin="5"
                                Command="{Binding EditCommand}"
                                Content="Sửa"
                                Style="{StaticResource MaterialDesignRaisedButton}" />
                            <TextBlock
                                Height="32"
                                VerticalAlignment="Top"
                                FontSize="12"
                                Foreground="Gray"
                                Text="{Binding EditMessage}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                        <StackPanel Width="110">
                            <Button
                                Width="100"
                                Margin="5"
                                Command="{Binding DeleteCommand}"
                                Content="Xóa"
                                Style="{StaticResource MaterialDesignRaisedButton}" />
                            <TextBlock
                                Height="32"
                                VerticalAlignment="Top"
                                FontSize="12"
                                Foreground="Gray"
                                Text="{Binding DeleteMessage}"
                                TextWrapping="Wrap" />
                        </StackPanel>

                    </StackPanel>
                </DockPanel>

            </StackPanel>
        </materialDesign:Card>

        <!--  Danh sách loại sảnh  -->
        <materialDesign:Card
            Grid.Row="3"
            Margin="10"
            Padding="5"
            VerticalAlignment="Stretch"
            BorderThickness="0.5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <!--  Danh sách sẽ chiếm phần còn lại và scroll  -->
                </Grid.RowDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Margin="0,0,10,0"
                    FontSize="14"
                    FontWeight="Bold"
                    Text="Danh sách loại sảnh" />

                <ListView
                    Grid.Row="1"
                    Margin="0"
                    VerticalAlignment="Stretch"
                    HorizontalContentAlignment="Left"
                    ItemsSource="{Binding List, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
                    <ListView.View>
                        <GridView>
                            <!--  Icon  -->
                            <GridViewColumn Width="50">
                                <GridViewColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="" />
                                    </DataTemplate>
                                </GridViewColumn.HeaderTemplate>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <materialDesign:PackIcon
                                            Width="24"
                                            Height="24"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center"
                                            Foreground="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}, Converter={StaticResource BoolToBrushConverter}}"
                                            Kind="RadioboxMarked" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <!--  Tên sảnh  -->
                            <GridViewColumn Width="150" Header="Tên sảnh">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Margin="20,0,0,0"
                                            HorizontalAlignment="Left"
                                            Text="{Binding TenSanh}"
                                            TextAlignment="Left" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <!--  Loại sảnh  -->
                            <GridViewColumn Width="200" Header="Loại sảnh">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Margin="20,0,0,0"
                                            HorizontalAlignment="Left"
                                            Text="{Binding LOAISANH.TenLoaiSanh}"
                                            TextAlignment="Left" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <!--  Đơn giá bàn tối thiểu  -->
                            <GridViewColumn Width="200" Header="Đơn giá bàn tối thiểu">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Margin="20,0,0,0"
                                            HorizontalAlignment="Left"
                                            Text="{Binding LOAISANH.DonGiaBanToiThieu, StringFormat=N0}"
                                            TextAlignment="Left" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <!--  Số lượng bàn tối đa  -->
                            <GridViewColumn Width="120" Header="Số lượng bàn tối đa">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Margin="20,0,0,0"
                                            HorizontalAlignment="Left"
                                            Text="{Binding SoLuongBanToiDa}"
                                            TextAlignment="Left" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <!--  Ghi chú  -->
                            <GridViewColumn Width="200" Header="Ghi chú">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Margin="20,0,0,0"
                                            HorizontalAlignment="Left"
                                            Text="{Binding GhiChu}"
                                            TextAlignment="Left" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
