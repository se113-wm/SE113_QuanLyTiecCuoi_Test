<UserControl x:Class="QuanLyTiecCuoi.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#FFF9F5">

    <UserControl.Resources>
        <DropShadowEffect x:Key="ShadowEffect" Color="Black" Direction="320" ShadowDepth="6" BlurRadius="10" Opacity="0.15" />
        <LinearGradientBrush x:Key="GradientBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FFE2F0CB" Offset="0.0" />
            <GradientStop Color="#FFB5EAEA" Offset="1.0" />
        </LinearGradientBrush>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- Phần text -->
        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="10" Effect="{StaticResource ShadowEffect}">
            <TextBlock Text="Quản Lý Tiệc Cưới"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="#4A7C59"
                       TextWrapping="Wrap"
                       Margin="0,0,0,15" />

            <TextBlock Text="Quản lý dịch vụ, món ăn và địa điểm tổ chức."
                       FontSize="18"
                       Foreground="#6B8E23"
                       TextWrapping="Wrap" />

            <TextBlock Text="Tối ưu vận hành - Phát triển kinh doanh."
                       Margin="0,10,0,0"
                       FontSize="16"
                       FontStyle="Italic"
                       Foreground="#8FBC8F"
                       TextWrapping="Wrap" />
        </StackPanel>

        <!-- Phần hình và khung -->
        <Border Grid.Column="1" CornerRadius="25" Background="{StaticResource GradientBackground}" Padding="20"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Effect="{StaticResource ShadowEffect}">
            <Border.RenderTransform>
                <ScaleTransform x:Name="ImageScale" ScaleX="1" ScaleY="1" />
            </Border.RenderTransform>
            <Border.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="ImageScale" Storyboard.TargetProperty="ScaleX" To="1.07" Duration="0:0:0.25" />
                            <DoubleAnimation Storyboard.TargetName="ImageScale" Storyboard.TargetProperty="ScaleY" To="1.07" Duration="0:0:0.25" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="ImageScale" Storyboard.TargetProperty="ScaleX" To="1" Duration="0:0:0.25" />
                            <DoubleAnimation Storyboard.TargetName="ImageScale" Storyboard.TargetProperty="ScaleY" To="1" Duration="0:0:0.25" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>

            <!-- Sử dụng Pack URI để đảm bảo WPF tìm đúng ảnh, và kiểm tra Build Action của ảnh là Resource -->
            <Image Source="/Image/wedding.jpg"
                   Stretch="Uniform"
                   Width="460"
                   Height="320"
                   RenderOptions.BitmapScalingMode="HighQuality" />
        </Border>
    </Grid>
</UserControl>