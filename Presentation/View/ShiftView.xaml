<UserControl
    x:Class="QuanLyTiecCuoi.Presentation.View.ShiftView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:local="clr-namespace:QuanLyTiecCuoi"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Name="ShiftViewControl">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--  Tên trang  -->
        <TextBlock
            Grid.Row="0"
            Margin="10"
            HorizontalAlignment="Center"
            FontSize="20"
            FontWeight="Bold"
            Text="Quản lý ca"
            AutomationProperties.AutomationId="ShiftPageTitle"/>
        <materialDesign:Card
            Grid.Row="1"
            Margin="10"
            Padding="10"
            BorderThickness="0.5"
            AutomationProperties.AutomationId="ShiftDetailsCard">
            <StackPanel>
                <!--  Tiêu đề + Nút Reset sát nhau  -->
                <StackPanel
                    Margin="0,0,0,0"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <TextBlock
                        VerticalAlignment="Center"
                        FontSize="14"
                        FontWeight="Bold"
                        Text="Thông tin ca"/>
                    <Button
                        Width="36"
                        Height="36"
                        Margin="5,0,0,0"
                        Background="{DynamicResource PrimaryHueMidBrush}"
                        Command="{Binding ResetCommand}"
                        Foreground="Purple"
                        Style="{StaticResource MaterialDesignToolButton}"
                        ToolTip="Reset thông tin"
                        AutomationProperties.AutomationId="ResetButton">
                        <materialDesign:PackIcon
                            Width="20"
                            Height="20"
                            Kind="Restore"/>
                    </Button>
                </StackPanel>
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <!--  Tên ca  -->
                    <TextBox
                        Grid.Column="0"
                        Margin="5"
                        materialDesign:HintAssist.Hint="Tên ca"
                        Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                        Text="{Binding ShiftName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        AutomationProperties.AutomationId="ShiftNameTextBox"/>
                    <!--  Thời gian bắt đầu ca  -->
                    <materialDesign:TimePicker
                        Grid.Column="1"
                        Margin="5"
                        materialDesign:HintAssist.Hint="Thời gian bắt đầu ca"
                        Is24Hours="True"
                        SelectedTime="{Binding StartTimeDateTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource MaterialDesignFloatingHintTimePicker}"
                        AutomationProperties.AutomationId="StartTimePicker"/>

                    <!--  Thời gian kết thúc ca  -->
                    <materialDesign:TimePicker
                        Grid.Column="2"
                        Margin="5"
                        materialDesign:HintAssist.Hint="Thời gian kết thúc ca"
                        Is24Hours="True"
                        SelectedTime="{Binding EndTimeDateTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource MaterialDesignFloatingHintTimePicker}"
                        AutomationProperties.AutomationId="EndTimePicker"/>
                </Grid>
            </StackPanel>
        </materialDesign:Card>
        <!--  Nút & tìm kiếm  -->
        <materialDesign:Card
            Grid.Row="2"
            Margin="10"
            Padding="10"
            BorderThickness="0.5"
            AutomationProperties.AutomationId="ActionsCard">
            <StackPanel>
                <TextBlock
                    Margin="0,0,10,0"
                    FontSize="14"
                    FontWeight="Bold"
                    Text="Thao tác"/>
                <DockPanel VerticalAlignment="Top"
                        LastChildFill="False">
                    <!--  Tìm kiếm  -->
                    <StackPanel
                        VerticalAlignment="Top"
                        DockPanel.Dock="Left"
                        Orientation="Horizontal">
                        <ComboBox
                            Width="200"
                            Margin="5"
                            materialDesign:HintAssist.Hint="Tìm theo"
                            ItemsSource="{Binding SearchProperties}"
                            SelectedItem="{Binding SelectedSearchProperty, Mode=TwoWay}"
                            Style="{StaticResource MaterialDesignOutlinedComboBox}"
                            AutomationProperties.AutomationId="SearchPropertyComboBox"/>
                        <TextBox
                            Width="300"
                            Margin="5"
                            materialDesign:HintAssist.Hint="Tìm kiếm..."
                            Style="{StaticResource MaterialDesignOutlinedTextBox}"
                            Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource CurrencyFormatConverter}}"
                            AutomationProperties.AutomationId="SearchTextBox"/>
                    </StackPanel>

                    <StackPanel
                        Margin="0,0,0,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        DockPanel.Dock="Right"
                        Orientation="Horizontal">
                        <!--  ComboBox chọn thao tác  -->
                        <ComboBox
                            Width="200"
                            Margin="0,0,5,14"
                            materialDesign:HintAssist.Hint="Chọn thao tác"
                            ItemsSource="{Binding ActionList}"
                            SelectedItem="{Binding SelectedAction, Mode=TwoWay}"
                            Style="{StaticResource MaterialDesignOutlinedComboBox}"
                            AutomationProperties.AutomationId="ActionComboBox"/>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <!--  Nút Thêm  -->
                            <StackPanel
                                Width="110"
                                Margin="0,0,0,0"
                                Visibility="{Binding IsAdding, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Button
                                    Width="100"
                                    Margin="5"
                                    Command="{Binding AddCommand}"
                                    Content="Thêm"
                                    Style="{StaticResource MaterialDesignRaisedButton}"
                                    AutomationProperties.AutomationId="AddButton"/>
                                <TextBlock
                                    Height="32"
                                    FontSize="10"
                                    Foreground="Gray"
                                    Text="{Binding AddMessage}"
                                    TextWrapping="Wrap"
                                    AutomationProperties.AutomationId="AddMessage"/>
                            </StackPanel>

                            <!--  Nút Sửa  -->
                            <StackPanel
                                Width="110"
                                Margin="0,0,0,0"
                                Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Button
                                    Width="100"
                                    Margin="5"
                                    Command="{Binding EditCommand}"
                                    Content="Sửa"
                                    Style="{StaticResource MaterialDesignRaisedButton}"
                                    AutomationProperties.AutomationId="EditButton"/>
                                <TextBlock
                                    Height="32"
                                    VerticalAlignment="Top"
                                    FontSize="10"
                                    Foreground="Gray"
                                    Text="{Binding EditMessage}"
                                    TextWrapping="Wrap"
                                    AutomationProperties.AutomationId="EditMessage"/>
                            </StackPanel>

                            <!--  Nút Xóa  -->
                            <StackPanel
                                Width="150"
                                Margin="0,0,0,0"
                                Visibility="{Binding IsDeleting, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Button
                                    Width="100"
                                    Margin="5"
                                    Command="{Binding DeleteCommand}"
                                    Content="Xóa"
                                    Style="{StaticResource MaterialDesignRaisedButton}"
                                    AutomationProperties.AutomationId="DeleteButton"/>
                                <TextBlock
                                    Height="32"
                                    VerticalAlignment="Top"
                                    FontSize="10"
                                    Foreground="Gray"
                                    Text="{Binding DeleteMessage}"
                                    TextWrapping="Wrap"
                                    AutomationProperties.AutomationId="DeleteMessage"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </DockPanel>

            </StackPanel>
        </materialDesign:Card>



        <!--  Danh sách ca  -->
        <materialDesign:Card
            Grid.Row="3"
            Margin="10"
            Padding="5"
            VerticalAlignment="Stretch"
            BorderThickness="0.5"
            AutomationProperties.AutomationId="ShiftListCard">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <!--  Danh sách sẽ chiếm phần còn lại và scroll  -->
                </Grid.RowDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Margin="0,0,10,0"
                    FontSize="14"
                    FontWeight="Bold"
                    Text="Danh sách ca"/>

                <ListView
                    Grid.Row="1"
                    Margin="0"
                    VerticalAlignment="Stretch"
                    HorizontalContentAlignment="Left"
                    ItemsSource="{Binding ShiftList, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                    AutomationProperties.AutomationId="ShiftListView">
                    <ListView.View>
                        <GridView>
                            <!--  Icon  -->
                            <GridViewColumn Width="50">
                                <GridViewColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text=""/>
                                    </DataTemplate>
                                </GridViewColumn.HeaderTemplate>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <materialDesign:PackIcon
                                            Width="24"
                                            Height="24"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center"
                                            Foreground="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}, Converter={StaticResource BoolToBrushConverter}}"
                                            Kind="RadioboxMarked"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!--  Tên ca  -->
                            <GridViewColumn Width="200"
                                    Header="Tên ca">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Margin="20,0,0,0"
                                            HorizontalAlignment="Left"
                                            Text="{Binding ShiftName}"
                                            TextAlignment="Left"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!--  Thời gian bắt đầu ca  -->
                            <GridViewColumn Width="200"
                                    Header="Thời gian bắt đầu">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Margin="20,0,0,0"
                                            HorizontalAlignment="Left"
                                            Text="{Binding StartTime, StringFormat={}{0:hh\:mm}}"
                                            TextAlignment="Left"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!--  Thời gian kết thúc ca  -->
                            <GridViewColumn Width="200"
                                    Header="Thời gian kết thúc">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Margin="20,0,0,0"
                                            HorizontalAlignment="Left"
                                            Text="{Binding EndTime, StringFormat={}{0:hh\:mm}}"
                                            TextAlignment="Left"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>