<Window
    x:Class="QuanLyTiecCuoi.Presentation.View.ServiceDetailItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Name="ServiceItemWindow"
    Title="Chọn dịch vụ"
    Width="900"
    Height="600"
    WindowStartupLocation="CenterScreen">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Tiêu đề  -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,16"
            HorizontalAlignment="Center"
            FontSize="22"
            FontWeight="Bold"
            Text="Chọn dịch vụ" />

        <!--  Tìm kiếm  -->
        <StackPanel
            Grid.Row="1"
            VerticalAlignment="Top"
            Orientation="Horizontal">
            <ComboBox
                Width="200"
                Margin="5"
                materialDesign:HintAssist.Hint="Tìm theo"
                ItemsSource="{Binding SearchProperties}"
                SelectedItem="{Binding SelectedSearchProperty, Mode=TwoWay}"
                Style="{StaticResource MaterialDesignOutlinedComboBox}" />
            <TextBox
                Width="300"
                Margin="5"
                materialDesign:HintAssist.Hint="Tìm kiếm..."
                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <!--  Danh sách dịch vụ dạng thẻ  -->
        <ListBox
            Grid.Row="2"
            Margin="0"
            HorizontalContentAlignment="Stretch"
            Background="Transparent"
            BorderThickness="0"
            ItemsSource="{Binding ServiceList, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            ScrollViewer.CanContentScroll="False"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            SelectedItem="{Binding SelectedService, Mode=TwoWay}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border
                        Width="200"
                        Height="140"
                        Margin="10"
                        Padding="12"
                        Background="{DynamicResource MaterialDesignPaper}"
                        BorderBrush="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}, Converter={StaticResource BoolToBrushConverter}, ConverterParameter='Highlight'}"
                        BorderThickness="2"
                        CornerRadius="10">
                        <StackPanel>
                            <TextBlock
                                Margin="0,0,0,4"
                                FontSize="16"
                                FontWeight="Bold"
                                Text="{Binding TenDichVu}" />
                            <TextBlock
                                Margin="0,0,0,4"
                                FontWeight="SemiBold"
                                Foreground="DarkGreen"
                                Text="{Binding DonGia, StringFormat='Giá: {0:N0} đ'}" />
                            <TextBlock
                                FontSize="13"
                                Foreground="Gray"
                                Text="{Binding GhiChu}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!--  Nút xác nhận và hủy  -->
        <StackPanel
            Grid.Row="3"
            Margin="0,20,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="120"
                Margin="0,0,10,0"
                Command="{Binding ConfirmCommand}"
                CommandParameter="{Binding ElementName=ServiceItemWindow}"
                Content="Xác nhận"
                IsDefault="True"
                Style="{StaticResource MaterialDesignRaisedButton}" />
            <Button
                Width="120"
                Command="{Binding CancelCommand}"
                CommandParameter="{Binding ElementName=ServiceItemWindow}"
                Content="Hủy"
                IsCancel="True"
                Style="{StaticResource MaterialDesignOutlinedButton}" />
        </StackPanel>
    </Grid>
</Window>