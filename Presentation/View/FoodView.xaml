<UserControl x:Class="QuanLyTiecCuoi.Presentation.View.FoodView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyTiecCuoi.Presentation.View"
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Grid.Row="0" Text="Quản lý món ăn"
                   FontSize="20" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0 0 0 10"/>
        <materialDesign:Card Grid.Row="1" Margin="0 0 0 10" Padding="10" BorderThickness="0.5">
            <StackPanel>
                <TextBlock Text="Thông tin chi tiết" FontSize="14" FontWeight="Bold" Margin="0 0 0 10"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1.5*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                      Text="{Binding TenMonAn, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      materialDesign:HintAssist.Hint="Tên món ăn"
                      Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                      Margin="5"/>

                    <TextBox Grid.Column="1"
                      Text="{Binding DonGia, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      materialDesign:HintAssist.Hint="Đơn giá"
                      Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                      Margin="5"/>

                    <TextBox Grid.Column="2"
                      Text="{Binding GhiChu, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                      materialDesign:HintAssist.Hint="Ghi chú"
                      Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                      Margin="5"/>
                </Grid>
            </StackPanel>
        </materialDesign:Card>
        <!-- Khu chức năng: tìm kiếm + nút -->
        <materialDesign:Card Grid.Row="2" Padding="10" Margin="0 0 0 10">
            <DockPanel>
                <!-- Khu tìm kiếm -->
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <ComboBox Width="150"
                      ItemsSource="{Binding SearchProperties}"
                      SelectedItem="{Binding SelectedSearchProperty}"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      materialDesign:HintAssist.Hint="Tìm theo"
                      Margin="5"/>
                    <TextBox Width="300"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     materialDesign:HintAssist.Hint="Tìm kiếm..."
                     Margin="5"/>
                </StackPanel>

                <!-- Khu nút thao tác và thông báo -->
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Top">
                    <!-- Thêm -->
                    <StackPanel Margin="5" Width="100">
                        <Button Content="Thêm" Command="{Binding AddCommand}" />
                        <TextBlock Text="{Binding AddMessage}"
                           Foreground="Gray"
                           FontSize="11"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Margin="0,3,0,0"/>
                    </StackPanel>

                    <!-- Sửa -->
                    <StackPanel Margin="5" Width="100">
                        <Button Content="Sửa" Command="{Binding EditCommand}" />
                        <TextBlock Text="{Binding EditMessage}"
                           Foreground="Gray"
                           FontSize="11"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Margin="0,3,0,0"/>
                    </StackPanel>

                    <!-- Xóa -->
                    <StackPanel Margin="5" Width="100">
                        <Button Content="Xóa" Command="{Binding DeleteCommand}" />
                        <TextBlock Text="{Binding DeleteMessage}"
                           Foreground="Gray"
                           FontSize="11"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Margin="0,3,0,0"/>
                    </StackPanel>
                </StackPanel>
            </DockPanel>
        </materialDesign:Card>


        <!-- Bảng món ăn -->
        <materialDesign:Card Grid.Row="3" Padding="10">
            <StackPanel>
                <TextBlock Text="Danh sách món ăn"
                           FontSize="16" FontWeight="Bold"
                           Margin="0 0 0 10"/>
                <ListView ItemsSource="{Binding List}"
                          SelectedItem="{Binding SelectedItem}">
                    <ListView.View>
                        <GridView>
                            <!-- Icon -->
                            <GridViewColumn Width="50">
                                <GridViewColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="" />
                                    </DataTemplate>
                                </GridViewColumn.HeaderTemplate>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <materialDesign:PackIcon Kind="RadioboxMarked"
                                                                 Width="24"
                                                                 Height="24"
                                                                 VerticalAlignment="Center"
                                                                 HorizontalAlignment="Left"
                                                                 Foreground="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}, Converter={StaticResource BoolToBrushConverter}}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Tên Món Ăn" Width="200" DisplayMemberBinding="{Binding TenMonAn}"/>
                            <GridViewColumn Header="Đơn Giá" Width="200">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DonGia, StringFormat=N0}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Ghi Chú" Width="200" DisplayMemberBinding="{Binding GhiChu}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
