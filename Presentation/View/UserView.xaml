<UserControl
    x:Class="QuanLyTiecCuoi.Presentation.View.UserView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:local="clr-namespace:QuanLyTiecCuoi"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Tên trang  -->
        <TextBlock
            Grid.Row="0"
            Margin="10"
            HorizontalAlignment="Center"
            FontSize="20"
            FontWeight="Bold"
            Text="Quản lý danh sách người dùng" />

        <!--  Thông tin chi tiết  -->
        <materialDesign:Card
            Grid.Row="1"
            Margin="10"
            Padding="10"
            BorderThickness="0.5">
            <StackPanel>
                <TextBlock
                    Margin="0,0,0,10"
                    FontSize="14"
                    FontWeight="Bold"
                    Text="Thông tin chi tiết" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>

                    <!--  Tên đăng nhập  -->
                    <TextBox
                        Grid.Column="0"
                        Margin="5"
                        materialDesign:HintAssist.Hint="Tên đăng nhập"
                        Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                        Text="{Binding TenDangNhap, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    
                    <!--  Họ tên  -->
                    <TextBox
                        Grid.Column="1"
                        Margin="5"
                        materialDesign:HintAssist.Hint="Họ tên"
                        Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                        Text="{Binding HoTen, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <!-- Email -->
                    <TextBox
                        Grid.Column="2"
                        Margin="5"
                        materialDesign:HintAssist.Hint="Email"
                        Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                        Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" />
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*" />
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="3*" />
                    </Grid.ColumnDefinitions>


                    <!-- Mật khẩu -->
                    <TextBox
                        Grid.Column="0"
                        Margin="5"
                        materialDesign:HintAssist.Hint="Mật khẩu mới"
                        Text="{Binding MatKhauMoi, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.Style>
                            <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignFloatingHintTextBox}">
                                <Setter Property="IsReadOnly" Value="True"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsChecked, ElementName=PasswordCheckBox}" Value="True">
                                        <Setter Property="IsReadOnly" Value="False"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <!-- Đối mật khẩu (CheckBox) -->
                    <StackPanel
                        Grid.Column="1"
                        Orientation="Horizontal" 
                        VerticalAlignment="Center">
                        <CheckBox 
                            x:Name="PasswordCheckBox"
                            Margin="0,0,5,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Bottom"
                            IsChecked="{Binding isChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock
                            HorizontalAlignment="Left"
                            FontSize ="10"
                            Text="Nhập mật khẩu mới"
                            Foreground="Gray"/>
                    </StackPanel>

                    <!--  Mã nhóm (ComboBox)  -->
                    <ComboBox
                        Grid.Column="2"
                        Margin="5"
                        materialDesign:HintAssist.Hint="Nhóm người dùng"
                        DisplayMemberPath="TenNhom"
                        ItemsSource="{Binding UserTypes}"
                        SelectedItem="{Binding SelectedUserType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource MaterialDesignFloatingHintComboBox}" />
                </Grid>
            </StackPanel>
        </materialDesign:Card>

        <!--  Nút & tìm kiếm  -->
        <materialDesign:Card
            Grid.Row="2"
            Margin="10"
            Padding="10"
            BorderThickness="0.5">
            <StackPanel>
                <TextBlock
                    Margin="0,0,10,0"
                    FontSize="14"
                    FontWeight="Bold"
                    Text="Thao tác" />
                <DockPanel VerticalAlignment="Top" LastChildFill="False">
                    <!--  Tìm kiếm  -->
                    <StackPanel
                        VerticalAlignment="Top"
                        DockPanel.Dock="Left"
                        Orientation="Horizontal">
                        <ComboBox
                            Width="200"
                            Margin="5"
                            materialDesign:HintAssist.Hint="Tìm theo"
                            ItemsSource="{Binding SearchProperties}"
                            SelectedItem="{Binding SelectedSearchProperty, Mode=TwoWay}"
                            Style="{StaticResource MaterialDesignOutlinedComboBox}" />
                        <TextBox
                            Width="300"
                            Margin="5"
                            materialDesign:HintAssist.Hint="Tìm kiếm..."
                            Style="{StaticResource MaterialDesignOutlinedTextBox}"
                            Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource CurrencyFormatConverter}}" />
                    </StackPanel>

                    <StackPanel
                        Margin="0,0,0,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        DockPanel.Dock="Right"
                        Orientation="Horizontal">
                        <StackPanel Width="110" Margin="0,0,20,0">
                            <Button
                                Width="100"
                                Margin="5"
                                Command="{Binding AddCommand}"
                                Content="Thêm"
                                Style="{StaticResource MaterialDesignRaisedButton}" />
                            <TextBlock
                                Height="32"
                                FontSize="10"
                                Foreground="Gray"
                                Text="{Binding AddMessage}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                        <StackPanel Width="110" Margin="0,0,20,0">
                            <Button
                                Width="100"
                                Margin="5"
                                Command="{Binding EditCommand}"
                                Content="Sửa"
                                Style="{StaticResource MaterialDesignRaisedButton}" />
                            <TextBlock
                                Height="32"
                                VerticalAlignment="Top"
                                FontSize="10"
                                Foreground="Gray"
                                Text="{Binding EditMessage}"
                                TextWrapping="Wrap" />
                        </StackPanel>
                        <StackPanel Width="110">
                            <Button
                                Width="100"
                                Margin="5"
                                Command="{Binding DeleteCommand}"
                                Content="Xóa"
                                Style="{StaticResource MaterialDesignRaisedButton}" />
                            <TextBlock
                                Height="32"
                                VerticalAlignment="Top"
                                FontSize="10"
                                Foreground="Gray"
                                Text="{Binding DeleteMessage}"
                                TextWrapping="Wrap" />
                        </StackPanel>

                    </StackPanel>
                </DockPanel>

            </StackPanel>
        </materialDesign:Card>

        <!--  Danh sách các người dùng  -->
        <materialDesign:Card
            Grid.Row="3"
            Margin="10"
            Padding="5"
            VerticalAlignment="Stretch"
            BorderThickness="0.5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <!--  Danh sách sẽ chiếm phần còn lại và scroll  -->
                </Grid.RowDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Margin="0,0,10,0"
                    FontSize="14"
                    FontWeight="Bold"
                    Text="Danh sách các người dùng" />

                <ListView
                    Grid.Row="1"
                    Margin="0"
                    VerticalAlignment="Stretch"
                    HorizontalContentAlignment="Left"
                    ItemsSource="{Binding List, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
                    <ListView.View>
                        <GridView>
                            <!--  Icon  -->
                            <GridViewColumn Width="50">
                                <GridViewColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="" />
                                    </DataTemplate>
                                </GridViewColumn.HeaderTemplate>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <materialDesign:PackIcon
                                            Width="24"
                                            Height="24"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center"
                                            Foreground="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}, Converter={StaticResource BoolToBrushConverter}}"
                                            Kind="RadioboxMarked" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <!--  Tên đăng nhập  -->
                            <GridViewColumn Width="150" Header="Tên đăng nhập">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Margin="20,0,0,0"
                                            HorizontalAlignment="Left"
                                            Text="{Binding TenDangNhap}"
                                            TextAlignment="Left" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <!--  Họ Tên  -->
                            <GridViewColumn Width="200" Header="Họ tên">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Margin="20,0,0,0"
                                            HorizontalAlignment="Left"
                                            Text="{Binding HoTen, StringFormat=N0}"
                                            TextAlignment="Left" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <!--  Nhóm người dùng  -->
                            <GridViewColumn Width="200" Header="Nhóm người dùng">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Margin="20,0,0,0"
                                            HorizontalAlignment="Left"
                                            Text="{Binding NhomNguoiDung.TenNhom}"
                                            TextAlignment="Left" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <!--  Email  -->
                            <GridViewColumn Width="200" Header="Email">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Margin="20,0,0,0"
                                            HorizontalAlignment="Left"
                                            Text="{Binding Email}"
                                            TextAlignment="Left" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
