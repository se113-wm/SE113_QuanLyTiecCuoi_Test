<UserControl
    x:Class="QuanLyTiecCuoi.Presentation.View.ReportView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:QuanLyTiecCuoi.Presentation.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="White"
    mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!--  Tiêu đề  -->
            <RowDefinition Height="Auto" />
            <!--  Bộ lọc  -->
            <RowDefinition Height="Auto" />
            <!--  Tổng doanh thu  -->
            <RowDefinition Height="*" />
            <!--  Bảng chi tiết  -->
            <RowDefinition Height="Auto" />
            <!--  Nút thao tác  -->
        </Grid.RowDefinitions>

        <!--  Tiêu đề  -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,20"
            HorizontalAlignment="Center"
            FontSize="26"
            FontWeight="Bold"
            Foreground="DarkSlateBlue"
            Text="BÁO CÁO DOANH SỐ"
            AutomationProperties.AutomationId="ReportPageTitle" />

        <!--  Bộ lọc  -->
        <StackPanel
            Grid.Row="1"
            Margin="0,0,0,15"
            Orientation="Vertical"
            AutomationProperties.AutomationId="FilterPanel">
            <!--  Dòng đầu tiên: Tiêu đề  -->
            <TextBlock
                Margin="0,0,0,10"
                FontSize="16"
                FontWeight="Bold"
                Text="Thao tác" />

            <!--  Dòng thứ hai: Các điều khiển chọn tháng, năm, nút xem  -->
            <StackPanel Orientation="Horizontal">
                <TextBlock
                    Margin="0,0,5,0"
                    VerticalAlignment="Center"
                    Text="Tháng:" />
                <ComboBox
                    Width="80"
                    Margin="0,0,15,0"
                    ItemsSource="{Binding Months}"
                    SelectedItem="{Binding SelectedMonth}"
                    AutomationProperties.AutomationId="MonthComboBox" />

                <TextBlock
                    Margin="0,0,5,0"
                    VerticalAlignment="Center"
                    Text="Năm:" />
                <ComboBox
                    Width="100"
                    Margin="0,0,15,0"
                    ItemsSource="{Binding Years}"
                    SelectedItem="{Binding SelectedYear}"
                    AutomationProperties.AutomationId="YearComboBox" />

                <Button
                    Width="120"
                    Command="{Binding LoadReportCommand}"
                    Content="Xem báo cáo"
                    AutomationProperties.AutomationId="LoadReportButton" />
            </StackPanel>
        </StackPanel>

        <!--  Tổng doanh thu  -->
        <TextBlock
            Grid.Row="2"
            Margin="0,0,0,10"
            FontSize="16"
            FontWeight="SemiBold"
            Foreground="DarkGreen"
            Text="{Binding TotalRevenue, StringFormat=Tổng Doanh Thu: {0:N0} VNĐ}"
            AutomationProperties.AutomationId="TotalRevenueTextBlock" />

        <!--  Bảng chi tiết  -->
        <DataGrid
            Grid.Row="3"
            Margin="0"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserReorderColumns="False"
            CanUserResizeColumns="False"
            CanUserResizeRows="False"
            CanUserSortColumns="False"
            FontSize="14"
            GridLinesVisibility="All"
            HeadersVisibility="Column"
            IsReadOnly="True"
            ItemsSource="{Binding ReportList}"
            VerticalScrollBarVisibility="Auto"
            AutomationProperties.AutomationId="ReportDataGrid">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="100"
                    Binding="{Binding RowNumber}"
                    Header="STT">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="TextAlignment" Value="Center" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn
                    Width="*"
                    Header="Ngày">
                    <DataGridTextColumn.Binding>
                        <MultiBinding StringFormat="{}{0}/{1}/{2}">
                            <Binding Path="Day" />
                            <Binding Path="Month" />
                            <Binding Path="Year" />
                        </MultiBinding>
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="TextAlignment" Value="Center" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding WeddingCount}"
                    Header="Số Lượng Tiệc">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="TextAlignment" Value="Center" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Revenue, StringFormat={}{0:N0} VNĐ}"
                    Header="Doanh Thu">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="TextAlignment" Value="Center" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Ratio, StringFormat={}{0:N2} %}"
                    Header="Tỉ Lệ (%)">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="TextAlignment" Value="Center" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Height" Value="35" />
                    <Setter Property="FontSize" Value="14" />
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="Background" Value="LightGray" />
                </Style>
            </DataGrid.Resources>
        </DataGrid>

        <!--  Nút thao tác  -->
        <StackPanel
            Grid.Row="4"
            Margin="0,15,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal"
            AutomationProperties.AutomationId="ActionButtonsPanel">
            <Button
                Width="100"
                Margin="5"
                Command="{Binding ExportPdfCommand}"
                Content="Xuất PDF"
                AutomationProperties.AutomationId="ExportPdfButton" />
            <Button
                Width="100"
                Margin="5"
                Command="{Binding ExportExcelCommand}"
                Content="Xuất Excel"
                AutomationProperties.AutomationId="ExportExcelButton" />
            <Button
                Width="120"
                Margin="5"
                Command="{Binding ShowChartCommand}"
                Content="Xem biểu đồ"
                AutomationProperties.AutomationId="ShowChartButton" />
        </StackPanel>
    </Grid>
</UserControl>
